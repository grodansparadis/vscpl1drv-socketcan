#!/usr/bin/make -f

%:
	dh $@ --with autoreconf

override_dh_auto_install:
	strip linux/vscpl1drv-socketcan.so
	install -D -m 0755 linux/vscpl1drv-socketcan.so $$(pwd)/debian/vscpl1drv-socketcan/srv/vscp/drivers/level1/vscpl1drv-socketcan.so
