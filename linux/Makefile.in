# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

# Package version
MAJOR_VERSION=@MAJOR_VERSION@
MINOR_VERSION=@MINOR_VERSION@
RELEASE_VERSION=@RELEASE_VERSION@
BUILD_VERSION=@BUILD_VERSION@
PACKAGE_VERSION=@PACKAGE_VERSION@

# STATIC = yes if a static build is requested.
STATIC=@STATIC@
VSCP_PROJ_BASE_DIR=@DESTDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

CFLAGS = @CFLAGS@ -fPIC -DCBC -I../_vscp_common -I../_vscp_common_general
CXXFLAGS = -D__LINUX__ -fPIC -D_REENTRANT @CXXFLAGS@
CPPFLAGS = -D__LINUX__ -fPIC @CPPFLAGS@ -fno-var-tracking-assignments -Iinclude -I../_vscp_common -I../_vscp_common_general

LDFLAGS = @LDFLAGS@
LIBS = @LIBS@
EXTRALIBS = @EXTRALIBS@

DLFLAGS = -g -shared -fPIC -Wl,--no-undefined
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib
LIB_SONAME = "vscpl1drv-socketcan"

### Variables: ###


CANALSOCKETCAN_OBJECTS = vscpl1drv-socketcan.o\
	socketcanobj.o\
	dllist.o

### Targets: ###

all: vscpl1drv-socketcan.so test

# static lib
lib:	vscpl1drv-socketcan.so
	ar rcs libvscpl1drv-socketcan.a $(CANALSOCKETCAN_OBJECTS)

test:  test.o $(CANALSOCKETCAN_OBJECTS)
	$(CXX) -o $@ test.o $(CANALSOCKETCAN_OBJECTS) $(LDFLAGS) @LIBS@ $(EXTRALIBS)

vscpl1drv-socketcan.so: $(CANALSOCKETCAN_OBJECTS)
	$(CXX) -o $@ $(CANALSOCKETCAN_OBJECTS) $(DLFLAGS) @LIBS@ $(EXTRALIBS)

vscpl1drv-socketcan.o: vscpl1drv-socketcan.cpp vscpl1drv-socketcan.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c vscpl1drv-socketcan.cpp -o $@

socketcanobj.o: socketcanobj.cpp socketcanobj.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c socketcanobj.cpp -o $@

guid.o: ../_vscp_common/guid.cpp ../_vscp_common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../_vscp_common/guid.cpp -o $@

dllist.o: ../_vscp_common_general/dllist.c ../_vscp_common_general/dllist.h
	$(CC) $(CFLAGS) -c ../_vscp_common_general/dllist.c -o $@

install: all
	$(STRIP) vscpl1drv-socketcan.so
	$(INSTALL_DIR) $(VSCP_PROJ_BASE_DIR)/drivers/level1/
	$(INSTALL_PROGRAM) vscpl1drv-socketcan.so $(VSCP_PROJ_BASE_DIR)/drivers/level1/

uninstall: all
	rm -rf $(VSCP_PROJ_BASE_DIR)/drivers/level2/vscpl1drv-socketcan.so

install-strip: install

man: ../man/vscpl1drv-socketcan.1.md
	pandoc -s -t man ../man/vscpl1drv-socketcan.1.md -o ../man/vscpl1drv-socketcan.1


deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../dist
	@mv *.deb ../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../_vscp_common/*.o
	rm -f ../_vscp_common_general/*.o
	rm -f vscpl1drv-socketcan.so
	rm -f test
	rm -rf ../common/*~
	rm -rf *~
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh Makefile

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
